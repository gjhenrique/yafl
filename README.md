# yafl

A fzf launcher

## Mode

``` toml
[modes.bookmark]
cache = 60
exec = "/opt/bin/yafl_bookmark"
prefix = "f"
name = "Search with bookmark"
```


Portable
Extensible
Cache will be there after one minute
